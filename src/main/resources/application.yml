spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/met?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: root #secuwowmail
    password: 1111 #Secuwow1@!
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
#        show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug

